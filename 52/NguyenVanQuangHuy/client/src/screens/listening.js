import React from 'react';
import { StyleSheet, View, Text } from 'react-native';
import { globalStyles } from '../styles/global';

export default function Listening() {
    return (
        <View style={globalStyles.container}>
            <Text>Listening Screen</Text>
        </View>
    );
}